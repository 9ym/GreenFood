<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.greenfood.board">
	<select id="sel" resultType="TestVo">
		select * from tbl_test
	</select>
	<insert id="ins">
		insert into tbl_test(sno, column1)
		values(2,'1002')
	</insert>
</mapper>